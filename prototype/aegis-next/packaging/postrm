#!/bin/sh
set -e

if [ "$1" = "purge" ]; then
    rm -rf /var/log/aegisbpf
    rm -f /etc/aegisbpf/aegisbpf-next.conf
fi

if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload || true
fi
